export enum WorkoutSource {
  MANUAL = 'MANUAL',
  AI_TEXT = 'AI_TEXT',
  AI_VOICE = 'AI_VOICE',
  AI_OCR = 'AI_OCR',
  IMPORT = 'IMPORT',
}

export enum WorkoutStatus {
  IN_PROGRESS = 'IN_PROGRESS',
  COMPLETED = 'COMPLETED',
  CANCELLED = 'CANCELLED',
}

export enum WeightUnit {
  KG = 'KG',
  LB = 'LB',
}
